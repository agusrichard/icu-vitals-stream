services:
  simulator:
    build:
      context: ../simulator
      dockerfile: Dockerfile
      target: runtime
    environment:
      - PATIENTS=3
      - BROKERS=kafka:9092
    restart: unless-stopped
